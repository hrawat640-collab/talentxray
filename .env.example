PORT=3000
# Serper.dev API key for /api/search-results (https://serper.dev) — server-side only
SERPER_API_KEY=

# Netlify build only — Render web service URL for /api proxy (no trailing slash)
# RENDER_API_URL=https://talentxray-api.onrender.com
